cmake_minimum_required(VERSION 3.0)
 
project(PavageBruit LANGUAGES CXX)

find_package(ASTex)

add_definitions(-DTEMPO_PATH=std::string\(\"${ASTEX_TEMPO_PATH}/\"\) )


add_executable(pavage_bruit "")
target_sources(pavage_bruit PRIVATE simplex.h)
target_link_libraries(pavage_bruit PRIVATE ASTex)

